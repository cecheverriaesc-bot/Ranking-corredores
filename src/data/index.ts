// Este archivo re-exporta todos los datos desde constants.ts (la fuente de verdad única).
// NUNCA editar datos directamente aquí. Usar el script ETL (scripts/etl_ranking.py) para actualizar.
export { MONTHLY_DATA, TEAMS, HISTORY_2025, NAMES_WITH_AGENDA, LAST_DB_UPDATE } from '../../constants';
